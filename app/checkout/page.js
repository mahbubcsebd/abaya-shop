import CheckoutPage from '../components/CheckoutPage';

export const metadata = {
    title: 'Abaya | Checkout',
};

const page = () => {
  return (
    <div>
        <CheckoutPage />
    </div>
  )
}

export default page